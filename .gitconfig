[user]
	name = yoskeoka
	email = y.okamoto@gemcook.com
[ghq]
	root = ~/src
[core]
	excludesfile = ~/.gitignore_global
	quotepath = false
	editor = vim
[commit]
	# template = ~/.stCommitMsg
[http]
	sslVersion = tlsv1.2
[filter "lfs"]
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
